# Example environment overrides for the demo Backstage app
# Copy to .env or .env.local at the repo root

# Frontend dev server port (Backstage app). The dev server may pick a different
# free port if this one is occupied; always use the printed URL.
PORT=3003

# In environments where PORT is pre-defined by the host, you can use DEMO_PORT
# to force the Backstage frontend dev server port used by the demo script.
#DEMO_PORT=3003

# Optional RuleHub config overrides
# Default hosted charts index (ensures Kyverno and Gatekeeper paths)
#RULEHUB_INDEX_URL=https://rulehub.github.io/rulehub-charts/plugin-index/index.json
# Or use a local index while developing rulehub-charts
#RULEHUB_INDEX_URL=http://localhost:8081/plugin-index/index.json
#RULEHUB_REPO_BASE_URL=https://github.com/rulehub/rulehub/tree/main/
#RULEHUB_SOURCE_BASE_URL=https://github.com/rulehub/rulehub-charts/tree/main/
#RULEHUB_INDEX_FILE=/absolute/path/to/rulehub-charts/dist/index.json
